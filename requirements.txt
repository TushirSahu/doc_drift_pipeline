qdrant-client>=1.7.0
ollama>=0.2.1
ragas>=0.1.7
langchain-core>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
langchain_ollama>=1.1.0
pyyaml>=6.0.0
datasets>=2.14.0
pandas>=2.0.0
pytest>=7.4.0
rank_bm25>=0.2.2
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.26.0,<0.28
sentence-transformers>=2.2.0   # cross-encoder reranker (pulls torch; heavy)
openai>=1.0.0                  # hosted model provider (OpenAI-compatible)
langchain-openai>=0.1.0       # Ragas eval LLM/embeddings for openai-compatible providers
langchain-community>=0.2.0    # Ragas HuggingFace (sentence-transformers) embeddings
psycopg[binary]>=3.1          # Postgres durable stores (optional; used when DATABASE_URL set)
psycopg_pool>=3.2             # pooled connections for per-request trace writes
redis>=5                      # shared rate-limit counter across instances (optional; REDIS_URL)
